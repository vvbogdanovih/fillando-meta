# Plan-0005 — додаток: повний перелік розбіжностей із макетом

> Робочий додаток до [`plan-0005-catalog-target-state.md`](plan-0005-catalog-target-state.md).
> Автоматична звірка 14 артбордів макета з кодом на `dev`, 2026-09-05: вісім
> аудиторів по кластерах, кожен звіт перевірено окремим скептиком. Наведено лише
> те, що **не** закрито: 111 пунктів зі 212. Закриті 101 тут не дублюються — рядок,
> закритий уже після звірки, лишається на місці з позначкою **код у dev**, щоб
> було видно, чим саме зрушив трекер.
>
> Стани: **немає** — коду немає; **частково** — щось є, але макет обіцяє більше;
> **дані** — код є, результату не буде до зміни даних або контенту.
> Шляхи вказані від кореня мета-репо. Файл живе, доки живий трекер, і
> викидається разом із ним.


## Категорія /filament

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| ~~немає~~ **код у dev** | S | Лічильник товарів біля H1 — «Філамент 156 товарів», «PLA Silk філамент 26 товарів» | Закрито (C3): `data.pagination.total` стоїть поруч із H1 на обох сторінках, у правильній українській множині |
| ~~немає~~ **код у dev** | S | Рядок над сіткою «Знайдено 42 товари за фільтром «Чорний»» | Закрито (C3+C4): рядок з'являється, щойно покупець щось звузив, і називає саме його фільтри — закріплені лендінгом до тексту не потрапляють, бо це сторінка, а не звуження |
| немає | M | Групи фільтрів як акордеон, «показати ще» при довгому списку значень | Акордеон, пошук усередині фільтра при >10 значень і «показати ще» — це Фаза 2 (docs/plans/plan-0002-catalog-seo-roadmap.md:149), коду немає |
| немає | M | Sticky-кнопка «Показати N товарів» на мобілці і чипи обраного з «очистити все» | Обидва елементи — Фаза 2 (plan-0002-catalog-seo-roadmap.md:149), не розпочата |
| немає | L | Фаза 2 — фасетні лічильники, range-фільтри, акордеон/чипи/sticky (нема ні TD, ні плану) | Виняток лише один: ColorFilter.tsx:90 уже показує число біля swatch-а (`option.count` із colorOptionsPipeline) — тобто лічильники є для кольору й відсутні для решти чотирьох фільтрів, які артборд категорії малює з «(24)». Треба: TD + план + 2 PR (BE: $facet-лічильники з виключенням власного виміру, числове сортування, range-агрегати; FE: акордеон, пошук при >10 значень, «показати ще», чипи, sticky на мобілці). |
| ~~частково~~ **код у dev** | M | Плитки «Популярні види» — сітка 5×2 карток із кольоровою крапкою, короткою назвою («PLA», «PETG», «TPU (Flex)») і лічильником («64 товари»), плюс замикальна плитка «Ще 4 види / усі лендінги» | Закрито (C2): `PopularLandings.tsx` — по п'ять у ряд, зображення плитки або стабільна кольорова крапка, коротка назва (H1 без хвостової згадки категорії) і лічильник товарів із `product_count`; десята плитка — «Ще N видів» |
| частково | S | Видимі breadcrumbs: «Головна › Філамент» на категорії і «Головна › Філамент › PLA Silk» на лендінгу | repos/fillando-fe/src/app/(root)/[category]/CatalogPage.tsx:123-131 будує масив крихт і додає третю лише за наявності `landing`; repos/fillando-fe/src/common/components/Breadcrumbs.tsx:20-60 з одного масиву віддає і `BreadcrumbList` JSON-LD, і видиму `<nav aria-label='Хлібні крихти'>`, тому розмітка не може розійтися з екраном |
| ~~частково~~ **код у dev** | S | Сітка товарів у 4 колонки на десктопі | Закрито (C10): `xl:grid-cols-4`, `EAGER_CARDS` піднято до 4, щоб перший ряд і далі вантажився не ліниво |
| частково | M | Колір стандартизований і фільтрується swatch'ами; назви у форматі «Чорний (Black)» | 50 варіантів (≈17% кольорової осі) не потраплять у swatch-фільтр і показуватимуться сирим українським написанням поруч із «Чорний (Black)». Ручна робота, описана в CATALOG_RELEASE.md:232: перебрати color-report.json, дописати синоніми або нові записи в seed-colors.js для кожного вартого написання, перепрогнати 3d і 3f (обидва ідемпотентні). Реально це не синоніми, а ~41 нових мультиколірних записів словника — див. DA |
| ~~частково~~ **код у dev / лишаються дані** | M | Кожна картка каталогу несе колір: назва «Sunlu PLA Silk — Золотий (Gold)» і кружечок кольору; вибір swatch'а звужує видачу | Код закрито (C1): назва двомовна, кружечок кольору на картці малюється тим самим правилом, що й swatch-и фільтра. Лишається дані: 5 товарів (32 SKU) без `color_id` покажуть сире написання й без кружечка, доки їх не розібрано вручну |
| дані | S | Чотири короткі фільтри «Тип пластику / Ефект поверхні / Армування / Серія» замість одного «Матеріал» на 29 значень | Міграцію прогнано лише на dev-базі: repos/fillando-be/scripts/migrations/reports/taxonomy-report.json — `"generated_for": "fillando-dev", "dry_run": false, "categories_to_change": 1`. Доки крок 3b з CATALOG_RELEASE.md не виконано на бойовій базі, сайдбар віддає старий «Матеріал». Плюс відкрите рішення №1: один товар із порожнім `material` випадає з усіх чотирьох вимірів |
| дані | S | Фільтр «Котушка в комплекті» зі значеннями «Так / Ні (рефіл)» | Значення `Ні (рефіл)` скрипт не пише ніколи (backfill-spool-included.js:39-40) — його треба проставити руками в адмінці. Товар із рефіл-варіантом FL-000253 міграція свідомо пропускає (`isRefillVariant`, рядки 42-52), тож поки відкрите рішення №3, фільтр має лише одне реально досяжне значення, а /filament/refill порожній |
| дані | M | П'ять міграційних задач Plan-0004 (16–20), яких ніхто не виконував на проді | Скрипти написані й перевірені на копії, але на проді не запускались і запуститись не можуть, доки не зроблено реліз dev→main обох репо (крок 3f взагалі забороняється до того, як фронт із показом «Чорний (Black)» уже в проді). Це не «код відсутній», а «дані ще не змінені» — і саме через це артборди категорії/товару/лендінга покупцю не покажуть нічого нового. |
| дані | S | Три дані-дефекти з CATALOG_RELEASE §0, які не належать жодному плану | Потрібні ручні правки в адмінці/базі до або одразу після міграцій: проставити «Матеріал = PETG» і перепрогнати 3b; злити або розвести два 'Candy'-варіанти; перевірити приховані пари такої ж форми скриптом із §0. Інакше один товар випадає з усіх нових фільтрів і з усіх лендінгів, а інший заблокований для редагування. |
| дані | M | П'ятий фільтр «Котушка в комплекті» з двома вибірними значеннями — покупець може попросити «покажи тільки з котушкою» або «тільки рефіли» | Артборд показує два чекбокси, код віддасть один («Так»). Один чекбокс, який нічого не відсіює, гірший за відсутній: покупець клікає й нічого не змінюється. Розблоковується тим самим кроком, що DATA-02/DATA-05 — щойно з'явиться хоч один товар зі значенням «Ні (рефіл)», фільтр стане осмисленим. Симетрію backfill сам TD-0002 називає обов'язковою (TD-0002:261-268), тож видаляти атрибут не можна — треба додати другу сторо |
| дані | S | Вибір «Так» показує весь асортимент із котушкою — TD-0002:250-252 стверджує, що «Так» — правда про весь наявний асортимент, крім одного PETG Translucent | Не задокументовано ніде. Наслідок для покупця: щойно він тикає «Котушка в комплекті: Так», з видачі зникає ВЕСЬ товар Bambu Lab PETG Translucent — усі 9 SKU, включно з 8 звичайними котушковими кольорами, які до фільтра підпадають. Артборд Category такого не передбачає. Лікується тим самим розділенням рефілу (DATA-05): після нього материнський товар отримає «Так», а рефіл — «Ні (рефіл)». До того — або розділити, або в |
| дані | M | Мультиколірні філаменти представлені окремими swatch'ами з кількома hex-стопами: «Gold Silk» (2 кольори), «Tri-Silk Forest» (3 кольори), «Rainbow» (5 кольорів); у Category — кружечок «Мультиколір» із conic-gradient | Усі 15 родин таки з'являться в сайдбарі, тож ряд swatch'ів виглядатиме повним — але «Мультиколір» покаже 2 варіанти замість ~43. AdminColors-артборд обіцяє записи з 2/3/5 стопами; у seed їх майже немає. Ручна робота: завести в словнику мультиколірні записи з hex_stops (Tri-Silk-и, Dual-Silk-и, Rainbow R1–R4, TR-1–TR-4), потім 3d + 3f. Це найбільша частина роботи з DATA-06 і ніде не оцінена. |
| дані | S | Кожен товар каталогу підпадає під один із чотирьох нових фільтрів і під відповідний лендінг; «Матеріал» із 29 значень зникає | Один товар (Kingroon PETG CoPET 3 кг) не отримає ні polymer, ні finish, ні reinforcement, ні series. Наслідок для покупця: товар зникає з видачі, щойно застосовано будь-який із чотирьох нових фільтрів, і не з'явиться на /filament/petg. При цьому «Матеріал» з required_attributes уже прибрано (derive-material-taxonomy.js:199-212), тож старого шляху до нього теж не буде — лишиться тільки нефільтрований лістинг і пошук.  |

## Лендінг

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| ~~немає~~ **код у dev** | S | Колонка «Контент» показує галочкою «готовий» / трикутником «порожній», де текст ще не написано | Закрито (D3): колонка «Контент» у `LandingTable.tsx` — галочка «готовий» / трикутник «порожній», похідне від `hasContent` (обидва тексти + хоча б одна пара FAQ). Міряє текст, а не розмітку: Quill лишає `<p></p>` після очищення редактора |
| ~~немає~~ **код у dev** | M | Колонка «Товарів» — скільки товарів підпадає під закріплені фільтри кожного лендінга (64, 38, 26, 14, 6, 5, 9) | Закрито (D3): `product_count` у `GET /landings/admin` — один `$facet` із гілкою на лендінг, матчить так само, як `findCatalogItems`. Нуль друкується destructive-кольором: саме такий лендінг API відмовляється публікувати |
| ~~немає~~ **код у dev** | S | Пояснювальний абзац над таблицею: лендінг — індексована сторінка каталогу із закріпленими фільтрами, власним H1, текстом і FAQ; категорії лишаються плоскими, це окрема сутність, а не підкатегорія | Закрито (D7): абзац над таблицею в `Landings.tsx` |
| немає | M | SEO-гігієна: сторінки краулятся, sitemap повний, індексовані адреси не втрачаються | 242 проіндексовані адреси товарів після кроку 3f почнуть віддавати 404 без 301 — це прямо суперечить духу блоку «SEO-гігієна» на Main, хоч сам артборд редиректів і не обіцяє. Рішення власника зафіксоване двічі (CATALOG_RELEASE.md:218, plan-0004 §6 «Slug варіантів змінюються без 301 — рішення власника, не ре-літигувати»), тож дія тут одна: свідомо підтвердити рішення перед 3f, знаючи цифру 242. Якщо рішення зміниться  |
| ~~частково~~ **код у dev** | M | Рядок чипів над сіткою: «Тип пластику: PLA» і «Ефект: Silk» із іконкою замка, знімний чип «Колір: Золото ✕» і підпис «закріплені фільтри лендінга зняти не можна · додаткові — можна» | Закрито (C4): `ActiveFilterChips.tsx` — закріплені з іконкою замка й без ✕, обрані покупцем знімні, плюс підпис. На мобілці це єдине місце, де закріплені фільтри взагалі видно |
| ~~частково~~ **код у dev** | S | Компонування лендінга: вступний абзац на всю ширину під H1, а внизу — дві картки поруч («Чому PLA Silk» і «Часті запитання») | Закрито (C8): вступ під H1 на всю ширину, сітка без сайдбара, внизу дві картки — SEO-текст (несе власний h2) і FAQ. Фільтри лендінга лишились досяжні через drawer на будь-якій ширині, інакше чипи могли б лише знімати |
| ~~частково~~ **код у dev** | M | Список — широка таблиця з колонками URL / H1 / Закріплені фільтри / Товарів / Контент / Статус / Порядок | Закрито (D3): `LandingTable.tsx` — таблиця на всю ширину з колонками Адреса / H1 / Закріплені фільтри / Товарів / Контент / Статус / Порядок / Дії |
| частково | S | Лічильник у шапці «Показано 7 з 14 · активних 11» | Половину зроблено (D7): у шапці стоїть «14 · активних 11». «Показано N з» **свідомо не зроблено** — списку ніщо не фільтрує й не пагінує, тож воно завжди читалося б «14 з 14». Закриється разом із фільтром списку або викресленням із макета |
| ~~частково~~ **код у dev** | M | Повноекранна форма редагування у дві колонки (зміст ліворуч, «Закріплені фільтри» і «Публікація» праворуч) із заголовком «Редагування: PLA Silk філамент» та кнопками Скасувати/Зберегти вгорі | Закрито (D5): `LandingForm.tsx` — повноекранна форма у дві колонки (зміст ліворуч, «Закріплені фільтри» і «Публікація» праворуч), заголовок «Редагування: <H1>», кнопки Скасувати/Зберегти в шапці |
| ~~частково~~ **код у dev** | S | Підказка «Санітизується на записі — так само, як описи товарів» | Закрито (D7): підказка під редакторами тексту |
| частково | S | FAQ-редактор: динамічний список карток «Питання N» з полями питання/відповідь, кнопками «Додати» і видалення | repos/fillando-fe/src/app/admin/landings/_components/LandingForm.tsx:91 useFieldArray({ control, name: 'faq' }); 252-264 заголовок «FAQ (n)» і кнопка «Додати питання» → faqArray.append({ q:'', a:'' }); 265-298 картки з Input aria-label={`Питання ${index+1}`}, Textarea «Відповідь» і кошиком faqArray.remove(index); валідація непорожніх пар — landings.schema.ts:14-17 |
| ~~частково~~ **код у dev** | M | Закріплені фільтри як окремі картки «Фільтр 1 / Фільтр 2» з двома випадайками — «Атрибут» (Тип пластику) і «Значення» (PLA) — кнопкою «Додати фільтр» і видаленням кожного | Закрито (D5): картки «Фільтр N» із парою «Атрибут» (Select людськими назвами) / «Значення», кнопкою «Додати фільтр» і видаленням кожної. «Значення» лишається мультивибором, а не одним випадайком артборда: у даних є багатозначні фільтри (`reinforcement: CF, GF`), яких один випадайок не виражає |
| ~~частково~~ **код у dev** | M | Заборонено публікувати лендінг, під який підпадає 0 товарів (і про це є попередження) | Закрито (D6): сервер відмовляє 409 на публікацію лендінга з 0 товарів — перевірка на стані, який лишить збереження, тож звуження фільтрів уже опублікованого теж не пройде. Форма блокує кнопку раніше, за тим самим лічильником |
| ~~частково~~ **код у dev** | S | Підказка й поведінка: на сайті закріплені фільтри не можна зняти; додаткові користувач може додати — тоді сторінка стає noindex | Закрито (D7): підказка під картками фільтрів |
| частково | M | Поле «Зображення плитки» — завантаження файлу JPEG/PNG/WebP для плитки лендінга | Половину зроблено (D5): поле `image` у формі й у zod-схемі, завантаження через спільний S3-presign (`UploadEntityType.LANDING` на беку). Лишається рендер картинки в плитках «Популярні види» — це C2, доти поле заповнюється, але ніде не видно |
| частково | L | 14 індексованих SEO-лендінгів (/filament/pla, /filament/pla-silk, /filament/carbon…) із власним H1, вступним текстом над сіткою, блоком «Чому PLA Silk» під нею і FAQ-акордеоном; в AdminLandings — колонка «контент: готовий», 5 із 7 рядків «Активний» | Написати вручну через /admin/landings для кожного з 14 лендінгів: title, meta_description, intro_html, bottom_html, мінімум 2-3 FAQ-пари, зображення плитки, order — і перевести status у active. Це ~14 × (2 тексти + FAQ) авторської роботи, нуль коду. Доки цього немає: /filament/pla-silk → 404, блок «Популярні види» на /filament не рендериться взагалі (CatalogPage.tsx:190 `popularLandings.length > 0 &&`), sitemap.ts:43 |
| дані | L | 14 лендінгів реально відкриваються і показують свій текст (/filament/pla-silk із описом, порадами по температурі, FAQ на три питання) | Треба написати копію для кожного лендінга в /admin/landings і опублікувати — це крок 4.2 у repos/fillando-be/src/docs/CATALOG_RELEASE.md. Плюс /filament/refill матчить 0 товарів (відкрите рішення №3: рефіл — це варіант FL-000253, а не окремий товар), тож із 14 сьогодні публікабельні 13 |
| дані | M | Перехресні посилання на суміжні лендінги внизу блоку («PLA Matte», «Звичайний PLA», «Філамент, що світиться») | Або редактор вписує ці посилання руками в `bottom_html` кожного з 14 лендінгів, або потрібен код перелінковки (у TD-0002 його немає; перелінковка згадана у Фазі 3 — plan-0002-catalog-seo-roadmap.md:162) |
| дані | L | 14 лендінгів існують, у 11 з них статус «Активний», а колонка «Контент» показує «готовий» — тобто текст, meta й FAQ написані | Прогнати seed-landings.js (після derive-material-taxonomy.js і backfill-spool-included.js), потім вручну написати H1/Title/meta/вступ/SEO-текст/FAQ кожному з 14 і перевести в «Опубліковано». До цього моменту код працює, але покупець і Google не бачать жодного лендінга — і колонка «Контент» на всіх 14 показувала б «порожній» |
| дані | L | Написання контенту 14 лендінгів (не покрито жодною задачею плану) | Немає задачі, оцінки, власника й дедлайну на 14 текстів (h1, title, meta_description, intro_html, bottom_html, FAQ на кожен). Доки їх не написано, /filament/pla і решта 13 адрес віддають 404 (задача 35: draft → notFound), тобто головна SEO-обіцянка артборда Landing не працює навіть після повного релізу і всіх міграцій. |
| дані | M | 14-й лендінг /filament/refill («Філамент-рефіл — намотка без котушки») — така сама індексована сторінка, як решта; критерій приймання Plan-0004 §6: «кожен із 14 лендінгів дає непорожню видачу» | Поки жоден товар не має `spool_included = Ні (рефіл)`, лендінг матчить 0 варіантів і публікувати його не можна навіть із написаним текстом. Розблоковується лише DATA-05 (виділення рефілу в окремий товар) + ручне проставлення атрибута. Тобто з 14 обіцяних лендінгів досяжні максимум 13. |
| дані | S | Адмін відкриває будь-який товар, правит поля й зберігає | Товар не можна перейменувати, доки дублі не злиті або не отримали різні v_value — а перейменування знадобиться щонайменше для DATA-06/DATA-08 (переклад назв варіантів у «Чорний (Black)»-формат). Це не баг коду: 409 — свідома заміна тихого напів-перейменування (комміт a3f0a7f). Ручна робота: злити FL-000157 і FL-000162 або дати другому інше значення варіанта. CATALOG_RELEASE.md:27-38 додатково вимагає ПЕРЕД деплоєм пр |

## Сторінка товару

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| немає | M | Реальний бренд виробника окремим видимим чипом над H1 («Sunlu») | Виробник у БД є (Product.vendor_id — repos/fillando-be/src/database/mongoose/schemas/product.schema.ts:48-49; Vendor.name — vendor.schema.ts:6-7), бракує vendor_name у відповіді GET /products/by-slug/:slug (той самий патерн, що вже є для category_name) і чипа на FE. Опосередковано бренд зараз видно лише як рядок «Виробник» у нижній таблиці атрибутів |
| немає | M | У Product JSON-LD brand — реальний виробник (Sunlu / Kingroon), а не «Fillando» | Потрібне vendor_name у by-slug (крок 3 TD-0006 §таблиця робіт) і заміна SITE_NAME на нього з фолбеком; поки не зроблено, Google Shopping бачить усі 43 товари як бренд «Fillando» |
| немає | S | productGroupID у розмітці — щоб Google склеїв кольори одного товару в одну групу варіантів | Немає productGroupID (= product.id при siblings.length > 1), а разом з ним і решти батчу 1 TD-0006: offers.itemCondition = NewCondition, offers.url = canonical, offers.priceValidUntil (+90 днів), itemDefectReturnFees у hasMerchantReturnPolicy |
| немає | M | Блок доставки на сторінці товару: «Нова Пошта — орієнтовно ₴105, доставка 1–3 дні» з підписом, що сума порахована за вагою 1.2 кг | Треба новий видимий блок з оцінкою вартості й строку доставки; він же залежить від prod-11 (без ваги нема чого рахувати) |
| немає | L | Доставка рахується за вагою товару (котушка 1 кг + пакування ≈ 1.2 кг), а не фіксовані ₴97 на все | Потрібні: ProductVariant.weight_g + DTO/адмінка, бекфіл ваги, віддача weight_g у by-slug і в кошику, вагова таблиця ставок замість константи 97 (TD-0006 §5.4, кроки 1/3/4/8). Це ж поле — єдиний спосіб зробити доставку рефілу дешевшою на вагу котушки, як обіцяє артборд Refill |
| ~~частково~~ **код у dev** | M | Та сама нова назва кольору «Золотий (Gold)» скрізь, де покупець бачить товар | Закрито (C1) у джерелі: `ProductService.variantName` і міграція пишуть «<товар> — Золотий (Gold)», тож кошик, снапшот замовлення й лист успадковують форму, нічого не приєднуючи. Картка каталогу додатково переписує стару збережену форму й кладе в кошик той самий рядок, що й сторінка товару |
| ~~частково~~ **код у dev** | M | Перемикач кольору — ряд із 6 круглих swatch-ів із заливкою кольору, підказкою з назвою при наведенні та зеленим обведенням активного | Закрито (C5): `VariantSwitcher.tsx` — swatch-и там, де кожен варіант має колір зі словника, інакше лишається випадайка; приглушений стан «немає в наявності» збережено, хоч артборд його й не показує |
| частково | S | Артикул FIL-0342 видно покупцю і він же поїхав у розмітку як sku | Додати `sku: variant.sku` в Product JSON-LD (дані вже на клієнті, зміна на один рядок). Реальний формат артикула у цій базі — FL-000253, а не FIL-0342 з макета |
| частково | S | availability у розмітці рахує не лише залишок, а й статус товару (архів → Discontinued) | Немає гілки status === 'archived' → https://schema.org/Discontinued. Сьогодні це ще не помилка в продакшн-сенсі, бо бек взагалі не віддає неактивні варіанти, але як тільки архівна сторінка почне повертати 200 (arch-01), розмітка збреше про наявність |
| дані | S | Таблиця характеристик із новими рядками: Тип пластику, Ефект поверхні, Серія, Котушка в комплекті, Вага філаменту | Рядки з'являться тільки після прогону обох міграцій на бойовій базі; окремого атрибута «Вага філаменту» жодна міграція не створює — він або вже є в даних як частина старого набору, або його треба заводити руками. Значків «нове» біля рядків у коді немає (це анотація макета, не UI) |
| дані | M | Короткий чистий H1 «Sunlu PLA Silk — Золотий (Gold)» | Щоб H1 виглядав як на макеті, треба перейменувати товари в контенті (і бренд винести в чип за prod-05). Увага: перейменування товару регенерує slug кожного варіанта — repos/fillando-be/src/modules/product/product.service.ts:312-318, тому це редиректо-небезпечна операція, а не косметика |

## Товар-рефіл

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| ~~немає~~ **код у dev** | S | У тексті попередження — клікабельне «оберіть варіант на котушці», що веде на спулений варіант | Закрито (C9): посилання під нотаткою веде на спулений варіант і показує його ціну |
| ~~немає~~ **код у dev** | S | Чорний бейдж «Без котушки» поверх фото товару | Закрито (C9): бейдж на фото, вмикається атрибутом `spool_included` після сплиту або маркером у v_value до нього |
| ~~немає~~ **код у dev** | M | Поруч із ціною рефілу — довідка «на котушці — ₴579», щоб було видно різницю | Закрито (C9): ціна спуленого варіанта поруч із ціною рефілу, посиланням. Парність більше не евристика: `split-refill-products.js` пише `Product.spooled_product_id`, і `by-slug` віддає `spooled_counterpart` того самого кольору |
| ~~частково~~ **код у dev** | S | Рядок «Котушка в комплекті: Ні (рефіл)» у таблиці характеристик, виділений (жирний, помаранчевий) на тлі решти | Закрито в частині коду (C6): значення «Ні (рефіл)» виділяється напівжирним помаранчевим. Сам рядок з'явиться лише після `backfill-spool-included.js` на бойовій базі — це дані |
| дані | L | Рефіл — окремий товар зі своєю назвою «PETG Translucent Refill (без котушки) — Прозорий (Natural)», своїм артикулом, ціною і атрибутом spool_included = «Ні (рефіл)» | Екран артборда в такому вигляді неможливий без розділення даних. Зараз: H1 = «Філамент (пластик для 3D принтера) Bambu Lab PETG Translucent (напівпрозорий) 1,75 мм 1 кг — Clear Безбарвний Refill» (product.name + v_value, бо колір міграцією свідомо пропущено, тож variantLabel падає на v_value — color.utils.ts:37); spool_included належить товару, тому виставити «Ні (рефіл)» неможливо, не позначивши рефілами всі спулені |
| дані | M | Фільтр «Котушка в комплекті» у каталозі, яким рефіли можна відібрати (і лендінг /filament/refill) | Оскільки spool_included живе на товарі, а рефіл — варіант (refill-05), лендінг /filament/refill і значення «Ні (рефіл)» у фільтрі сьогодні дадуть або порожній список, або цілий спулений товар як рефіл. Фільтр стане правдивим лише після сплиту даних |
| дані | M | Розділення рефілу FL-000253 в окремий товар | Потрібна ручна операція в адмінці/скриптом: винести варіант FL-000253 у власний товар, проставити йому «Котушка в комплекті = Ні (рефіл)», після чого лендінг /filament/refill і callout ATTR_NOTES (задача 39) почнуть щось показувати. Коду міняти не треба — CATALOG_RELEASE прямо каже «makes all three work with no code change». Артборд Refill без цього не відтворюється взагалі. |
| дані | L | Рефіл — окремий товар: H1 містить «(без котушки)», бейдж «Без котушки» на фото, перший рядок таблиці атрибутів «Котушка в комплекті = Ні (рефіл)», реальна вага без ~230 г котушки, ціна ₴489 проти ₴579 на котушці, посилання «оберіть варіант на котушці» | Що саме не збігається з артбордом: (1) H1 буде «Bambu Lab PETG Translucent (напівпрозорий) 1,75 мм 1 кг — Clear Безбарвний Refill», а не «PETG Translucent Refill (без котушки) — Прозорий (Natural)»; (2) рядка «Котушка в комплекті = Ні (рефіл)» у таблиці не буде взагалі — товар пропущений backfill'ом і атрибута не має; (3) варіант пропущений і кольоровою міграцією, тож у нього немає color_id → немає ні «Прозорий (Natu |

## Архівний товар

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| немає | M | Архівний товар лишається доступним за прямим посиланням — 200 зі станом «знято з продажу», а не 404, щоб не ламати стару рекламу й закладки | Треба: у by-slug пускати ARCHIVED (лишаючи DRAFT недоступним), у FE розвести дві гілки — DRAFT → notFound(), ARCHIVED → 200 + robots {index:false, follow:true} + availability Discontinued (prod-09). Сьогодні ARCHIVED і DRAFT нерозрізненні: обидва 404 |
| немає | M | Візуальний стан архівної сторінки: сірий пілл «Знято з продажу», знебарвлене фото, перекреслена ціна, неактивна кнопка «Немає в продажу» | Потрібен окремий режим сторінки для ARCHIVED: свій напис «Знято з продажу», перекреслена ціна, grayscale-галерея, вимкнена кнопка й прихований степер кількості. Без arch-01 цей режим недосяжний |
| немає | S | Закриття чотирьох відкритих питань TD-0006 §8 | Питання 4 уже конфліктує з відвантаженим кодом: repos/fillando-be/src/database/mongoose/repositories/product-variant.repository.ts:138 віддає by-slug лише для status ACTIVE, тобто архівний товар сьогодні 404 — рівно те, чого артборд Archived («знято з продажу», а не 404) не хоче. Тобто рішення власника тут не просто «відкрите питання», а вимога змінити вже змерджену поведінку Plan-0003. |
| частково | S | Архівний товар зникає з каталогу, з пошуку, з sitemap і з прайс-листа | repos/fillando-be/src/database/mongoose/repositories/product-variant.repository.ts — status: ProductStatus.ACTIVE у findAllSlugs (:118, sitemap), countAll (:129, ключ кешу sitemap), findBySkuPrefix (:220) і findSearchResults (:239), у каталожних агрегаціях (:318, :397, :464, :564, :569, :601). Публічна проєкція прайс-листа теж активна-only — product-public.mappers.ts:96-120 у парі з тими ж match-стадіями |

## Чекаут: помилки

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| немає | M | Помилка складу показується біля тієї позиції кошика, якої стосується: під рядком «Sunlu PLA Silk — Золотий (Gold)» з іконкою-попередженням і червоною сумою позиції «× 4 · ₴2 196» | Потрібен машиночитний код помилки з бекенду (зараз лише текст `Only ${variant.stock} units available for SKU ${variant.sku}` — repos/fillando-be/src/modules/order/order.service.ts:172-176) або парсинг SKU→variant_id на фронті, стан «помилкових рядків», рендер повідомлення + іконки в `<li>` і destructive-стиль суми позиції. |
| частково | S | Текст складської помилки — українською, з підказкою що робити («зменште кількість, щоб оформити замовлення»), замість англійського «Only 3 units available for SKU FIL-0342» | Немає жодного мапінгу складських повідомлень на українську; артборд додатково вимагає інструктивного хвоста «— зменште кількість, щоб оформити замовлення», якого немає ні у FE, ні у BE. |
| частково | S | «Сторінка прокручується до першої помилки» — тобто після невдалого сабміту покупця підводить до місця, яке треба виправити | Для складської відмови (тобто саме для сценарію артборда) скролу немає взагалі: жоден елемент не отримує aria-invalid, помилка живе тільки в тості, тож `scrollFirstInvalidIntoView` не має до чого скролити і не викликається (onError не викликає її). |
| частково | S | Підпис поля «Знижковий купон» і плейсхолдер «Введіть код купона» | Локалізація зроблена, але формулювання інші за артборд: «Промокод» замість «Знижковий купон», плейсхолдер-приклад коду замість «Введіть код купона». |
| частково | M | Фікси аудиту (воронка замовлення + security) — покриття планом | docs/plans/plan-0003-security-hardening.md:3 — «Status: In Progress — код усіх 23 задач змерджено в dev обох репо». Вибірково перевірено на dev: repos/fillando-be/src/app.module.ts:58 ThrottlerModule.forRoot; src/modules/vendor/vendor.controller.ts:38,46,54 @Roles(Role.ADMIN); src/modules/order/order.service.ts:420 getPaymentStatusPublic; src/database/mongoose/repositories/product-variant.repository.ts:138 findOne({  |

## Оплата не пройшла

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| частково | S | Пояснення «Банк відхилив платіж — кошти не списано. Замовлення №1042 збережено, товари зарезервовані: можна спробувати ще раз або обрати інший спосіб оплати.» | Бракує хвоста «: можна спробувати ще раз або обрати інший спосіб оплати» (артборд явно підказує обидві дії текстом); номер замовлення не вбудований у речення, тож фраза «Замовлення збережено» звучить без прив'язки до конкретного №. |
| частково | L | «Товари зарезервовані» — покупцю обіцяють, що після відмови банку його позиції нікуди не подінуться до повтору оплати | Реального резерву стоку немає — між FAILED і повтором оплати ті самі одиниці може купити хтось інший, і повтор упаде на тій самій перевірці стоку. Або прибрати обіцянку з тексту, або зробити резервування (нове поле reserved/hold + TTL + врахування в наявності). |
| частково | S | Головна зелена кнопка «Повторити оплату карткою», яка знову веде на LiqPay без створення нового замовлення | Дрібниця: артборд малює статичну іконку кругової стрілки зліва від тексту — у коді іконки немає, Loader2 з'являється лише поки `retryMutation.isPending` (:295-297). |
| частково | M | Друга кнопка «Обрати інший спосіб оплати» як рівноцінна альтернатива повтору картки | Кнопка веде на сторінку контактів, а не до вибору способу оплати: жодного UI зміни payment_method для вже створеного замовлення немає (ні на success-сторінці, ні в кабінеті). Фактично це «напишіть менеджеру», а артборд обіцяє дію. |
| частково | M | Із екрана статусу завжди можна доплатити незавершене замовлення («можна спробувати ще раз») | Замовлення, що застрягло в PENDING (наприклад, коли POST /liqpay/checkout впав), не має жодного способу оплати з інтерфейсу — ні на success-сторінці, ні в /profile/orders; рятує лише лист/менеджер. Рішення свідомо відкладене (щоб не запустити другу сесію LiqPay поверх живої), але діри у воронці це не закриває. |

## Адмінка: кольори

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| ~~немає~~ **код у dev** | S | Біля нових пунктів меню (Colors, Landings, Google Feed) висить бейдж «нове» — зеленою капсулою | Закрито (D7) для Colors і Landings: `navLink` приймає необов'язковий `badge`, обидва пункти носять зелену капсулу «нове». Google Feed з'явиться разом із блоком E |
| немає | L | У сайдбарі окремим верхньорівневим пунктом (нижче акордеона Catalogue) з'являється «Google Feed» із бейджем «нове» | Уся Фаза 5 (TD-0006, Google Merchant Center) не розпочата: немає ні ендпоінта генерації фіда, ні адмін-екрана, ні пункту меню |
| ~~немає~~ **код у dev** | S | Колонка «Slug» — моноширинним шрифтом, щоб адмін бачив адресу кольору просто зі списку | Закрито (D1): колонка `Slug` у `ColorTable.tsx`, `font-mono text-xs` |
| ~~немає~~ **код у dev** | M | Колонка «Варіантів» — скільки варіантів товарів використовують цей колір (34, 22, 9 …), вирівняна праворуч | Закрито (D2): окремий ADMIN-ендпоінт `GET /colors/admin` віддає словник із `variant_count` — один `$group` по `color_id`; поле в zod-схемі `adminColorSchema`, колонка праворуч у `ColorList.tsx`. Рахує варіанти в усіх статусах, тобто той самий набір, що й guard видалення. Публічний `GET /colors` не змінився |
| ~~немає~~ **код у dev** | S | Колонка «Порядок» — числове значення order видно у кожному рядку (10, 20, 30 …), вирівняне праворуч | Закрито (D1): колонка `Порядок`, `text-right tabular-nums` |
| ~~частково~~ **код у dev** | S | Порядок пунктів усередині Catalogue: Products, Categories, Vendors, Colors, Landings | Закрито (D7): порядок у `catalogueItems` тепер такий, як в артборді |
| ~~частково~~ **код у dev** | M | Екран «Кольори» — це картка з таблицею словника на всю ширину контенту (thead із 9 колонок, рядки з border-bottom) | Закрито (D1): `Colors.tsx` — `Card` на всю ширину, `ColorTable.tsx` — `<table>` із 9 колонок і `border-b` на рядках. Master-detail розібрано: форма переїхала в модалку (див. «Діалог кольору»), бо таблиця на всю ширину й бічна панель не співіснують |
| частково | S | Перша колонка — круглий swatch 28px, зафарбований за hex_stops (суцільний / лінійний градієнт / конічний для multicolor) | repos/fillando-fe/src/app/admin/colors/_components/ColorList.tsx:74-79 — `<ColorSwatch hexStops={color.hex_stops} family={color.family} size={28} title={color.name_uk} />`; правило заливки — src/common/components/ColorSwatch.tsx:25-31: 1 стоп → суцільний, `family === 'multicolor'` → `conic-gradient(...)`, інакше `linear-gradient(135deg, ...)` |
| ~~частково~~ **код у dev** | S | Колонка «Name EN (канон)» — канонічна англійська назва окремою колонкою, набрана напівжирним | Закрито (D1): `name_en` — окрема колонка `font-medium text-gray-900`, `name_uk` — сусідня `text-gray-600`, тобто акцент такий, як в артборді |
| ~~частково~~ **код у dev** | S | Колонка «Кольори нитки» — по одному квадратному чипу 14px на кожен стоп плюс підпис (моноширинний «#111418» для одного стопа, «2 кольори»/«3 кольори»/«5 кольорів» для кількох) | Закрито (D1): `size-3.5` чип на кожен стоп у збереженому порядку, з hex у `title`; підпис — hex моноширинним для одного стопа, інакше `N кольори/кольорів` (2-4 / 5+) |
| частково | S | У шапці екрана — лічильник «Показано 8 з 48» (скільки видно з усього словника); окремого пошуку, фільтра чи пагінації артборд не показує | **Свідомо не зроблено в D1** і лишається відкритим: пагінації, пошуку й фільтра немає, тож «Показано N з M» завжди читалося б «Показано 103 з 103». Виводиться лише M. Закривається або разом із фільтром, або викресленням із макета |
| ~~частково~~ **код у dev** | S | Кнопка «Новий колір» у шапці екрана відкриває форму створення | Закрито (D1): кнопка в шапці картки називається «Новий колір» і відкриває модалку з `initial=null` |
| ~~частково~~ **код у dev** | S | Над таблицею — пояснювальний абзац: словник є єдиним джерелом правди для назв і фільтра, родина визначає групу swatch-фільтра, зміна родини перезаписує її на всіх варіантах цього кольору, кольорів нитки може бути скільки треба | Закрито (D7): абзац у `Colors.tsx`, і він прямо каже, що зміна родини масово перезаписує `color_family` — операція без відкату за звичайним на вигляд селектом |
| частково | M | Ручний розбір 49 нерозпізнаних написань кольору | Ітеративний після-міграційний цикл (розбір звіту → додавання синонімів → повторний прогін seed-colors + normalize-variant-colors) не оформлений окремою задачею й не має критерію завершення. Наслідок для артборда: swatch-фільтр кольору працюватиме, але приблизно 16% варіантів у нього не потраплять і будуть невидимі при фільтрації за кольором. |
| дані | M | Список показує заповнений словник (у макеті 48 кольорів: Black, White, Gray, Gold Silk, Tri-Silk Forest, Natural, Rainbow …) | Доки не прогнали seed-colors.js на цільовій базі, екран показує порожній словник; це крок даних, а не коду |
| дані | M | У словнику є багатоколірні записи: Gold Silk (2 стопи), Tri-Silk Forest (3), Rainbow (5) — саме вони демонструють градієнтні swatch-и | Багатоколірні кольори треба завести вручну в адмінці або дописати в сід — механіка готова, бракує саме записів |

## Діалог кольору

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| ~~частково~~ **код у dev** | M | Редагування кольору відбувається в модальному діалозі 512px по центру, поверх затемненого оверлея, з хрестиком у куті | Закрито разом із D1 (окремим пунктом блоку D не значилося, але таблиця на всю ширину інакше неможлива): `ColorForm.tsx` — `Dialog`/`DialogContent sm:max-w-lg` (512px) з оверлеєм і власним хрестиком; тіло форми скролиться, футер лишається на місці. Решта чотирьох рядків цієї секції — підказки, порядок полів, drag-and-drop, формулювання прев'ю — досі відкриті |
| ~~частково~~ **код у dev** | S | Два текстові поля назви поруч: «Name EN (канон)» з хінтом «Як у виробника — унікальне, використовується у slug» і «Назва укр» з хінтом «На сайті показуватиметься «Зелений Bambu (Bambu Green)»» | Закрито (D7): порядок полів як в артборді (канонічна англійська перша), обидва хінти на місці, другий показує живий прев'ю-рядок. Лишається одне: повідомлення про дубль усе ще приходить англійською з бекенду |
| частково | S | Порядок кольорів нитки змінюється перетягуванням — у кожному рядку є drag-хендл («Перетягуванням змінюється порядок») | Немає drag-and-drop; перевпорядкування працює, але жестом, якого артборд не показує. Бібліотека вже в проєкті, тож обсяг доробки малий |
| ~~частково~~ **код у dev** | S | Блок «Прев'ю на сайті» — круглий swatch, підпис «Прев'ю на сайті · 3 кольори», рядок «Зелений Bambu (Bambu Green)» і згенерований slug «bambu-green» | Закрито (D7): підпис «Прев'ю на сайті · N кольорів» у формулюванні артборда, під назвою — згенерований slug. Пояснення про градієнт переїхало до списку стопів, де стопи й редагуються |
| частково | S | Хінт «Перший колір — основний: саме він іде в товарний фід і як фолбек» | Текст присутній, але фід, на який він посилається, ще не існує (див. AC-04): жодного коду Google Merchant у жодному репо немає, тож обіцянка «іде в товарний фід» поки не підкріплена |

## Нові поля у формах

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| немає | M | У картці варіанта з'являється п'яте поле «Вага, г» (значення 1200) поруч із Ціна / Кількість / Артикул вендора / ID на Промі, позначене бейджем «нове». | Треба додати `weight_g: number\|null` у ProductVariant-схему, у create-variant.dto.ts / update-variant.dto.ts, перегенерувати openapi (`yarn spec:export`), додати поле в zod-схеми create/update і в обидва місця UI (VariantFieldsCard і VariantModal), плюс підказку під полем. |
| немає | M | Підказка під полем ваги: вага «живить розрахунок доставки» — тобто доставка перестає бути фіксованою сумою і рахується від ваги. | Потрібна таблиця вагових діапазонів (TD-0006 §5.4) і заміна статичної константи на обчислення від weight_g; ставки — плейсхолдери, доки не звірені з тарифами Нової Пошти (§8 п.2). |
| немає | S | Підказка під полем ваги: вага живить `shipping_weight` у Google-фіді. | Поле стає корисним тільки разом із білдером фіда (FEED-01); саме по собі weight_g нічого не дає Google. |
| немає | S | Для наявних товарів вагу проставить міграція, розпізнаючи її з назви («1kg», «3кг»); решту адмін вносить вручну. | Написати backfill-скрипт за наявним патерном (dry-run + звіт у scripts/migrations/reports/), але тільки після того, як з'явиться саме поле weight_g. |
| немає | M | У формі категорії з'являється ціла нова секція «SEO та Google Merchant» із двома полями: «Google-категорія (ID)» = 2496 і «Шлях таксономії» = Hardware > Tools > Milling & Cutting Machines > 3D Printer Filament. | Треба embedded-підсхема `{ id: number, path: string } \| null` з `_id:false` на Category, поле в create/update-category DTO, `yarn spec:export`, поле в zod-схемі FE і нова секція з двома інпутами у CategoryForm. |
| немає | S | Сірий інфо-блок під секцією: порожня Google-таксономія не блокує фід — товари категорії йдуть без `google_product_category`, а у звіті генерації з'являється попередження. | Попередження має генерувати FeedService під час збірки фіда; писати нічого, доки немає FEED-01. |
| частково | S | Жовтий інфо-блок: порожня вага нічого не блокує — товар просто йде у фід без `shipping_weight`, а на сторінці показується тариф за замовчуванням. | Логіка «пропустити поле, не впасти» має бути написана в google-shopping-feed.builder.ts і в обчисленні доставки; зараз писати нічого — обох місць не існує. |
| частково | S | Адмін бачить і редагує в формі всі обов'язкові атрибути категорії, зокрема «Ефект поверхні» | На двох товарах (PLA Matte Rainbow і PLA Silk Rainbow) другий рядок `finish` у формі невидимий: адмін бачить «Ефект поверхні: Matte», не бачить «Rainbow», не може його виправити чи видалити, а при збереженні той тихо їде далі. Фільтрація й лендінги від цього не страждають ($elemMatch трактує це як OR — derive-material-taxonomy.js:13-15), тобто дані правильні, а UI адмінки їх не вміє показати. Ніде не зафіксовано. Вип |

## Статус Google-фіда

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| немає | L | Існує сам Google Shopping XML-фід за публічним URL `/feeds/google-shopping.xml`, який показано в банері й відкриває кнопка «Відкрити XML». | Треба весь модуль із TD-0006 §5.3: feed.module.ts, feed.controller.ts, feed.service.ts (in-memory кеш XML), google-shopping-feed.builder.ts (RSS 2.0 + g:-namespace, xmlEscape/cdata), product-type.resolver.ts, feed.types.ts, нова вибірка `findActiveForFeed()` у product-variant.repository.ts і реєстрація в app.module.ts. |
| немає | M | Окремий екран адмінки «Google Feed» (бейдж «новий екран») — новий пункт навігації й новий роут. | Новий роут /admin/feed, пункт у catalogueItems сайдбара, константа UI_URLS.ADMIN.FEED, api-шар (feed.api.ts + zod-схема відповіді) — усе з нуля. |
| немає | S | Кнопка «Перегенерувати» — ручний запуск перегенерації фіда з адмінки. | ADMIN-guarded синхронний POST-ендпоінт, що повертає FeedGenerationSummary, плюс мутація і кнопка на фронті. |
| немає | M | Зелений банер стану: «Фід актуальний — згенеровано 12:04, 2 вересня», «Наступна автогенерація о 13:00 (щогодини)» — тобто крон-джоба з відомим часом останнього й наступного запуску. | feed-cron.service.ts зі щогодинним розкладом, overlap-guard як у PromSyncService, зберігання generatedAt/lastSummary у FeedService і ADMIN-ендпоінт /status, який їх віддає. |
| немає | S | Чотири лічильники згори: 142 товарів у фіді · 128 в наявності/14 немає · 4 попереджень · 3 виключено — тобто структурований підсумок генерації. | FeedGenerationSummary із лічильниками total/in_stock/out_of_stock/warnings/excluded, який повертають і POST /regenerate, і GET /status. |
| немає | M | Таблиця «Виключено з фіда» — SKU / назва / причина, з конкретними причинами: «статус: архівний», «немає жодного зображення», «статус: чернетка». | Правила виключення (status ≠ ACTIVE, нуль зображень, ціна ≤ 0 або відсутня, висяче посилання на product/category/vendor), їх накопичення у summary і таблиця на екрані. |
| немає | M | Список «Попередження» з чотирма типами: не заповнена вага, порожній опис, не заповнений обов'язковий атрибут категорії («Діаметр»), категорія без Google-таксономії — кожне з лічильником уражених товарів. | Це саме те місце, де required_attributes мали б отримати видимість без enforcement (TD-0006 §5.3): потрібен прохід по рядках фіда з накопиченням «м'яких» прогалин. |
| немає | S | Фід сегментує кампанії через custom labels 0..4 (категорія, бренд, бакет маржі, ціновий діапазон, швидкість продажів) — це те, заради чого екран узагалі має сенс для Shopping/PMax. | custom_label_0..3 пишуться разом із білдером; custom_label_4 — окремий fast-follow з агрегатом по замовленнях. |
| немає | M | Екран статусу фіда має сенс лише в парі з повним конверсійним трекінгом для PMax — GA4-події view_item / add_to_cart / begin_checkout / purchase поверх наявного Google Ads пікселя. | Треба NEXT_PUBLIC_GOOGLE_ANALYTICS_ID (env + build-args у Dockerfile.prod/docker-compose.prod.yml), другий `gtag('config')` в Analytics.tsx, новий модуль src/common/lib/ga4-events.ts і чотири точки виклику (ProductPage useEffect на variant.id, handleAddToCart, CheckoutPage, CheckoutSuccessContent поруч із наявним Ads-івентом). |
| немає | L | TD-0006 (Google Merchant Center) — покриття планом | Треба: ревʼю і затвердження TD-0006, написання implementation plan, потім реалізація 15 кроків §9 — модуль feed/ (схема, репозиторій, білдер XML, контролер, крон), поля ProductVariant.weight_g і Category.google_product_category + бекфіл ваги, vendor_name/weight_g у by-slug, три FE-батчі JSON-LD (brand, sku, itemCondition, productGroupID, priceValidUntil, доставка за вагою), GA4-обвʼязка, MERCHANT_FEED.md, адмін-екран |
| немає | M | Операційні кроки власника в кабінетах Google (поза кодом, поза планами) | Без цих кроків навіть повністю реалізований фід нікуди не піде: URL не зареєстрований у Merchant Center, GA4-property не існує, тож події не мають куди надсилатись. Потрібен трекер (у майбутньому плані Фази 5) із власником-виконавцем, бо це блокує приймання роботи. |

## Огляд змін

| Стан | Обсяг | Обіцянка макета | Що конкретно бракує |
|---|---|---|---|
| немає | S | Невиконані пункти всередині Plan-0003 (чекліст) | Чотири незакриті чекбокси — це не код, а реліз і документація. Найдорожчий із них — оновлення FRD: grep по docs/requirements/FRD.md не дає жодного входження «429», «Retry-After», «orders/lookup», «Оплата не пройшла», тобто джерело правди не знає про половину Plan-0003. |
| частково | L | TD-0002 (таксономія, кольори, лендінги) — покриття планом | План покриває TD-0002 повністю, але 5 задач із 42 (16–20: derive-material-taxonomy, required_attributes, backfill-spool-included, seed+normalize кольорів, seed-landings) мають статус «☐ прод» — прогнані лише на копії бази. Доки вони не виконані на проді, ЖОДНА обіцянка артборда TD-0002 не матеріалізується навіть після релізу коду: фільтрів polymer/finish/reinforcement/series у категорії немає, кольори не привʼязані,  |
| частково | S | Статуси планів не відповідають дійсності | Треба: синхронізувати індекс README з файлами планів; додати в Plan-0002 §3 позначки стану по фазах (Фаза 0 — код у dev, Фаза 1 — код у dev + 5 міграцій на проді, Фази 2–5 — не почато) або винести їх у окрему таблицю прогресу, інакше роадмап читається як «нічого не зроблено» і повторно планується вже зроблене. |
| частково | S | Статуси технічних дизайнів (TD) не відповідають дійсності | TD-0003 → Implemented. TD-0005 і TD-0006 треба провести через ревʼю до Approved (для TD-0006 це формальна передумова написання плану за його ж §9). TD-0007 лишається Draft свідомо (відкладено), це нормально, але варто це записати в статусі, а не тримати в памʼяті. |
| частково | S | Правила закриття планів із CONTRIBUTING — що треба зробити і чи дотримано | Жоден із двох планів наразі й не може бути закритий коректно — обидва не задеплоєні, тож Done ставити рано. Але порядок дій уже порушено в одному місці: FRD оновлено частково і не за фактом релізу, а за фактом мержу в dev. Додатково: правки документації ще не в git — у мета-репо `git status` показує 8 модифікованих файлів (CLAUDE.md, docs/README.md, TD-0002, plan-0003, plan-0004, runbooks, validate-env.sh), у repos/f |

## Поза екранами: плани, дані, процес

| Стан | Обсяг | Предмет | Деталі |
|---|---|---|---|
| немає | M | Таблиця характеристик — курований і впорядкований набір рядків (7 рядків на Product, 4 на Refill, «Котушка в комплекті» першим) | repos/fillando-fe/src/app/(root)/products/[slug]/ProductPage.tsx:488 друкує геть усі атрибути товару в порядку зберігання — `product.attributes.map(attr => {...})`, ні allowlist, ні сортування, ні групування. repos/fillando-be/scripts/migrations/derive-material-taxonomy.js:4-6 прямо каже, що нічого не видаляє: «Nothing is deleted: `material` stays on every product as its marketing name, and the derived triplets are a |
| немає | M | Сторінка рефілу знає свій спулений відповідник — показує його ціну («на котушці — ₴579») і веде на нього посиланням «оберіть варіант на котушці» | Аудитор у refill-02 і refill-04 радить брати спулений варіант із `siblings` («siblings уже є на відповіді»), але це працює лише доти, доки рефіл — варіант того самого товару. Щойно виконають його ж refill-05 (виділити FL-000253 в окремий Product), siblings перестануть його містити: repos/fillando-be/src/database/mongoose/repositories/product-variant.repository.ts:150-152 добирає сусідів строго за `{ product_id: varia |
| немає | M | Під заголовком діалогу — пояснювальний підзаголовок «Канонічна англійська назва від виробника, український переклад, родина для фільтра і скільки завгодно кольорів нитки» | repos/fillando-fe/src/app/admin/colors/_components/ColorForm.tsx:112-119 — у шапці форми лише `<h2 className='text-sm font-semibold text-gray-900'>{initial ? 'Редагувати колір' : 'Новий колір'}</h2>` і кнопка XIcon; жодного описового <p> під заголовком немає. |
| немає | S | Відкрите питання 1: `custom_label_2` (бакет маржі high/medium/low) буде публічно видимий будь-кому, хто відкриє URL фіда — власник має свідомо погодитись або попросити замінити цей label. | Рішення власника: лишити маржу в публічному фіді, замінити label на щось інше (лише категорія+бренд) чи прибрати. Без відповіді білдер фіда не можна дописати до кінця. |
| немає | S | Відкрите питання 3: конкретні id + path Google-таксономії для категорії «Філамент» — це вибір власника з опублікованої таксономії, не технічне рішення. Артборд AdminNewFields показує 2496 / «Hardware > Tools > Milling & Cutting Machines > 3D Printer Filament» як приклад. | Навіть після додавання поля (ANF-06) фід ітиме без g:google_product_category, доки власник не підбере й не впише значення — це рівно стан «data». |
| немає | M | Товари без залишку лишаються у фіді, а не виключаються з нього: 142 у фіді = 128 «в наявності» + 14 «немає», тоді як виключено лише 3 (архів / без фото / чернетка). Тобто нульовий стік — це g:availability=out_of_stock, а не причина виключення. | Ні фіда, ні лічильників немає (ls repos/fillando-be/src/modules — без feed; openapi.json — 72 шляхи, жодного /feeds). Єдиний наявний availability-сигнал живе на сторінці товару: repos/fillando-fe/src/app/(root)/products/[slug]/ProductPage.tsx:174-175 `availability: availableStock > 0 ? 'https://schema.org/InStock' : '…/OutOfStock'` — і його не можна перенести у фід як є: він рахується від залишку й ігнорує status (TD |
| немає | M | Обов'язкові атрибути категорії живлять g:product_type у фіді — попередження прямо каже, що без «Діаметра» «фільтр і product_type неповні». Тобто required_attributes отримують друге, фідове призначення поверх фільтрів. | repos/fillando-be/src/database/mongoose/schemas/category.schema.ts:5-19 — RequiredAttribute це key/label/filter_type/unit, суто метадані фільтра; значення живуть у product.attributes (k/l/v) і ніде не перевіряються на заповненість. Резолвера немає: `grep -rn "product_type\\|productType" src/` по BE — 0 хітів (TD-0006 §5.3 планує окремий product-type.resolver.ts). |
| немає | M | Порожній опис не виключає товар: у фід іде назва замість опису, а адмін бачить лише попередження «слабший сигнал релевантності». | Фолбек-логіка вже існує поруч — для метаданих сторінки: repos/fillando-fe/src/app/(root)/products/[slug]/page.tsx:32-33 `rawDescription = product.description?.html?.replace(/<[^>]*>/g,'').slice(0,155) ?? null` і `description = rawDescription ?? \`Купити ${title} у Fillando\``. У JSON-LD фолбека немає взагалі (ProductPage.tsx:167 віддає undefined), а для фіда немає ні тексту, ні споживача. |
| немає | M | XML — це попередньо згенерований знімок із зафіксованим моментом («згенеровано 12:04, 2 вересня»), який віддається Google до наступної регенерації, а не збірка на кожен запит; банер показує свіжість цього знімка. | Немає ні сервісу, ні місця для кешу/мітки часу: `grep -rn "FeedService\\|feed.service" ` по BE — 0 хітів. Найближчий наявний аналог — синхронний генератор без кешу: repos/fillando-be/src/modules/product/price-list/price-list.service.ts:43 `private generating = false` (лише overlap-guard, PDF будується щоразу наново й ніде не зберігається). Тобто «актуальний/застарілий» як стан не має де жити. |
| немає | M | Вага у полі — брутто: «Котушка + пакування», 1200 г для 1-кг котушки. Отже міграція, що розпізнає «1kg»/«3кг» з назви, має додавати надбавку за котушку й пакування, а не писати 1000. | Поля немає (`grep -rn "weight_g"` по обох репо — 0), міграції теж: `grep -rn "кг\\|kg" repos/fillando-be/scripts/migrations/*.js` — 0 хітів. Найближчий наявний патерн похідного backfill — scripts/migrations/backfill-spool-included.js (атрибут «Котушка в комплекті», src/common/utils/backfill-spool-included.migration.spec.ts:52,57), і він якраз показує, що «рефіл без котушки» — окремий випадок, для якого надбавка інша. |
| немає | M | Сторінка товару показує покупцеві бренд виробника окремим бейджем над назвою («Sunlu») і рядок доставки «Нова Пошта — орієнтовно ₴105, доставка 1–3 дні», порахований за вагою товару 1.2 кг замість фіксованих ₴97 | Артборд Product підписує обидва елементи як видимі («Sunlu — бренд виробника — і в розмітці для Google», «розраховано за вагою товару 1.2 кг … а не фіксовані ₴97 для всього»). У repos/fillando-fe/src/app/(root)/products/[slug]/ProductPage.tsx немає ні бейджа бренду, ні рядка доставки: слова vendor/brand/«Виробник»/«Нова Пошта» трапляються тільки у :169 `brand: { '@type': 'Brand', name: SITE_NAME }` і :176 `shippingDe |
| немає | M | AdminColors — таблиця словника з колонками Name EN (канон) / Назва укр / Slug / Родина / Кольори нитки / **Варіантів** / Порядок; заголовок «Показано 8 з 48» | `repos/fillando-fe/src/app/admin/colors/_components/ColorList.tsx` — це не таблиця, а вузький master-список: swatch, «name_uk (name_en)» (:83-85), бейдж родини (:88) і «N стоп.» (:92). Немає ні колонки Slug, ні «Порядок», ні — головне — «Варіантів». Лічильника використань не існує й на беку як публічної величини: `countByColorId` викликається рівно один раз, як guard на видалення (`repos/fillando-be/src/modules/color |
| ~~частково~~ **код у dev** | M | На лендінгу колонки фільтрів немає: під чипами йде сітка товарів на всю ширину 1280 px, а закріплені й додаткові фільтри виражені рядком чипів | Закрито (C8): сайдбар рендериться лише на категорії; на лендінгу сітка йде на всю ширину, а фільтри — у drawer |
| ~~частково~~ **код у dev** | M | Назва товару в картці каталогу — двомовна: «Kingroon PLA — Чорний (Black)», «Sunlu PLA Silk — Золотий (Gold)» (Main.dc.html теж: «назви у форматі «Чорний (Black)»») | Закрито (C1): картка друкує «Kingroon PLA — Чорний (Black)» і кладе в кошик той самий рядок |\| vValue; return `${productName} — ${suffix}``) і так само в міграції repos/fillando-be/scripts/migrations/normalize-variant-c |
| ~~частково~~ **код у dev** | M | Перемикач кольору (ряд swatch-ів) стоїть НАД степером кількості й кнопкою «Додати в кошик» — колір обирають до рішення про покупку, а не після нього | Закрито (C5): перемикач переїхав над блоком «Додати в кошик» |
| ~~частково~~ **код у dev** | M | Один рядок на одну характеристику — «Ефект поверхні: Silk» | Закрито (C6): багатозначні виміри зливаються в один рядок («Ефект поверхні: Silk, Rainbow»). До цього два записи давали два рядки з однаковим React-ключем |
| частково | M | «зменште кількість» — покупець може виправити кількість прямо в підсумку чекауту, і вона не перевищить залишок (артборд показує позицію з кількістю ×4 та інструкцію зменшити її на цьому ж екрані) | Степер є в кожному рядку підсумку: repos/fillando-fe/src/app/(root)/checkout/CheckoutPage.tsx:1181-1231 (кнопки «Зменшити кількість»/«Збільшити кількість» + числовий input), усі три виклики передають `line.stock` (:1191, :1211, :1223). Обмеження: `applyQuantity` (:392-400) — `const normalized = stock !== undefined ? Math.min(normalizedBase, stock) : normalizedBase`, а `stock` береться з відповіді кошика (:140 `stock: |
| частково | M | «Банк відхилив платіж — кошти не списано» — текст на екрані має бути правдою про те, що сталося з грошима й із замовленням | Для справжньої відмови твердження підкріплене: FAILED пишеться лише для LiqPay-статусів 'failure'/'error' (repos/fillando-be/src/modules/liqpay/liqpay.service.ts:17 `FAILED_STATUSES`, :121-143), а PAID додатково вимагає збігу суми й валюти (:125-134). Але той самий екран і той самий текст показуються для VOIDED: repos/fillando-fe/src/app/(root)/checkout/success/CheckoutSuccessContent.tsx:87-94 — `case 'FAILED': case  |
| частково | M | Екран має власну шапку: H1 «Кольори» на 24px поруч із зеленою капсулою «новий екран», а праворуч у тій самій шапці — лічильник і кнопка створення | Сторінка не має жодного заголовка рівня екрана: repos/fillando-fe/src/app/admin/layout.tsx:11-19 рендерить лише сайдбар + `{children}`, а repos/fillando-fe/src/app/admin/colors/Colors.tsx:28-66 одразу віддає split-view. Єдиний заголовок — усередині лівої колонки: repos/fillando-fe/src/app/admin/colors/_components/ColorList.tsx:49-52 `<h2 className='text-sm font-semibold text-gray-900'>Кольори<span className='ml-2 fon |
| частково | M | Під списком стопів — пояснення правила: «Один колір — суцільний swatch. Два й більше — градієнт у порядку, як тут: для Dual-Silk два, для Tri-Silk три, для градієнтних скільки треба (до 6)» | Правило заливки показане лише як підпис живого прев'ю й лише для поточного стану — repos/fillando-fe/src/app/admin/colors/_components/ColorForm.tsx:132-138 («Один стоп — суцільний колір» / «N стопів — конічний градієнт» / «N стопів — лінійний градієнт»), а межа 6 — тільки як лічильник у лейблі `Кольори ({stops.length}/6)` (рядок 195). Тексту про порядок стопів як джерело градієнта і про Dual-Silk/Tri-Silk у формі нем |
| ~~частково~~ **код у dev** | M | У списку лендінгів колонка «Закріплені фільтри» показує чипи людськими назвами атрибутів — «Тип пластику: PLA», «Ефект: Silk», «Армування: CF» | Закрито (D4): `landing-attributes.ts` будує мапу `attrKey → label` із `required_attributes` категорії, `LandingTable.tsx` і `PinnedFilters.tsx` читають ту саму мапу; невідомий ключ падає назад на сирий, а не на порожній чип |
| ~~частково~~ **код у dev** | M | Форма редагування поділена на п'ять карток-секцій із заголовками «Адреса та заголовки», «Контент», «FAQ», «Закріплені фільтри», «Публікація», причому дві останні винесені в праву колонку | Закрито (D5): `LandingForm.tsx` — п'ять `Section`-карток у сітці `lg:grid-cols-[1fr_380px]`, дві останні праворуч |
| частково | S | Відкрите питання 4: як обробляти ARCHIVED-товар — TD пропонує 200 + schema.org/Discontinued замість 404, щоб не зламати посадкову сторінку діючої PMax-кампанії; власник має підтвердити або дозволити простий 404. | Підтвердження власника, чи він завжди призупиняє кампанію одночасно з архівацією SKU. Від цього залежить, писати на фронті гілку Discontinued чи плоский notFound(). |
| частково | M | Плитки «Популярні види» на сторінці категорії — це картки з кольоровою крапкою й лічильником товарів («PLA — 64 товари», «PETG — 38 товарів», «Карбон (CF) — 14 товарів»), а остання плитка — «Ще 4 види · усі лендінги» | Артборд Category, renderVals(): tiles: [{ name: "PLA", count: "64 товари", dot: "#2C962C" }, … { name: "Ще 4 види", count: "усі лендінги", dot: "#e2e8f0" }]. У коді на dev плитка — плоске текстове посилання: repos/fillando-fe/src/app/(root)/[category]/page.tsx:111 передає лише `popularLandings={(landings ?? []).map(({ slug, h1 }) => ({ slug, h1 }))}`, а CatalogPage.tsx:190-207 рендерить `<Link …>{item.h1}</Link>` — б |
| частково | M | Екран «Оплата не пройшла» дає дві дії: «Повторити оплату карткою» і «Обрати інший спосіб оплати» | Артборд PayFailed малює другу кнопку як рівноцінну дію над замовленням. У коді repos/fillando-fe/src/app/(root)/checkout/success/CheckoutSuccessContent.tsx:300-302 ця кнопка є, але це `<Button asChild variant='outline'><Link href={UI_URLS.CONTACTS}>Обрати інший спосіб оплати</Link></Button>` — вона веде на сторінку контактів і не перемикає спосіб оплати вже створеного замовлення (жодного PATCH payment_method у FE нем |
| частково | M | Плитки «Популярні види» на /filament — це картки з кольоровим кружечком і лічильником товарів («PLA — 64 товари», «PETG — 38 товарів») плюс замикальна плитка «Ще 4 види — усі лендінги»; артборд Main називає їх «головним способом навігації замість простині чекбоксів» | Код віддає плоский ряд текстових пігулок без кружечка, без лічильника й без «ще N»: `repos/fillando-fe/src/app/(root)/[category]/CatalogPage.tsx:39` тип `popularLandings?: { slug: string; h1: string }[]`, :196-203 рендер `<Link …>{item.h1}</Link>`. Дані обрізаються ще на сервері: `[category]/page.tsx:111` `.map(({ slug, h1 }) => ({ slug, h1 }))`. Поля `image`, `price_min`, `price_max` у `Landing` (`repos/fillando-be/ |
| частково | M | Значення нових фільтрів у сайдбарі підписані по-людськи й списки короткі: «Армування: Вуглеволокно (CF) / Скловолокно (GF)», «Ефект поверхні»: Silk / Matte / Rainbow / Glow / Wood (5 значень) | Опції беруться сирими з `product.attributes.v` і друкуються без жодної мапи підписів: `repos/fillando-be/src/database/mongoose/repositories/product-variant.repository.ts:573-580` `$group { _id: '$product.attributes.k', values: { $addToSet: { $toString: '$product.attributes.v' } } }`; `repos/fillando-fe/src/app/(root)/[category]/components/AttributeFilter.tsx:49` — `{option}` як є (порівняй із `ColorFilter.tsx:11-27`, |
| дані | M | Слова «(без котушки)» зашиті в назву товару, тож вони їдуть у кошик, лист і Google-фід без жодного коду (анотація артборда Refill) | Транспорт справді готовий і коду не потребує: кошик віддає збережену назву варіанта — repos/fillando-be/src/modules/cart/cart.service.ts:34 `name: v.name`; замовлення знімає з неї снапшот — repos/fillando-be/src/modules/order/order.service.ts:183 `name: variant.name`; лист друкує цей снапшот — order.service.ts:301 `name: i.name` і :717 `name: item.name`. Але сама назва будується як product.name + українська назва кол |
| дані | S | Відкрите питання 2: вагові діапазони для обчислюваної доставки — реальний тільки якір ~1.3 кг / ₴97, решта ставок у TD є плейсхолдерами. | Звірка з чинними тарифами Нової Пошти й фіксація таблиці діапазонів у TD перед тим, як цифри потраплять у JSON-LD на проді. |
| дані | M | Унизу лендінга — блок перехресних посилань на суміжні лендінги («PLA Matte», «Звичайний PLA», «Філамент, що світиться») | Артборд Landing, рядки 96-98 (три `<a>`-пігулки в картці «Чому PLA Silk», перед FAQ). Роут лендінга не передає у сторінку жодного списку суміжних лендінгів: repos/fillando-fe/src/app/(root)/[category]/[landing]/page.tsx:101-113 віддає лише categorySlug / initialCategory / initialCatalog / pinnedFilters / landing, а CatalogPage.tsx:38 прямо коментує проп: «Published landings of this category, shown as entry tiles. Emp |
| дані | S | Товар живе у категорії «Філамент» так само, як решта 42 | Сьогодні непомітно, бо каталог фільтрує по `ProductVariant.category_id`, який має правильний тип (product-variant.repository.ts:562 `category_id: categoryObjectId`). Перший же код, що піде по товарах — фід Google Merchant TD-0006, звіт якості даних або будь-який адмінський лічильник по категорії — мовчки цей товар пропустить. Робота: одна правка типу в базі під час виправлення DATA-09 (той самий документ). Записано в |
| дані | S | Числа, за якими звірятимуть успіх міграцій (42/43 товари, 56 кольорів, 14 лендінгів, 13 непорожніх, ~244/292 кольори, 1 пропущений рефіл, одна колізія слагів) | Жодна цифра з §0/§3 не перевірена на продакшн-базі — у проді може бути інша кількість незматчених кольорів, інший набір дублів слагів (§0 вимагає знайти «every latent pair of #4's shape»), інші товари з порожнім material. Ручна робота перед деплоєм: прогнати dry-run усіх шести скриптів і скан колізій проти прод-бази, звірити зі списком очікуваних чисел і тільки тоді планувати обсяг ручного розбору DATA-06/DATA-08/DAT |
| дані | M | «Серія» — один із чотирьох нових робочих фільтрів (артборд Category показує Standard / High Speed / Plus як вибір, що звужує видачу) | `repos/fillando-be/scripts/migrations/reports/taxonomy-report.json` → `series=Standard: 37`, `series=High Speed: 2`, `series=Plus: 2`, `series=Lite: 1` при 42 змінених товарах. Одна опція покриває 88% каталогу, три інші — по 1-2 товари, а «Lite» артборд узагалі не передбачає. Це та сама хвороба, що аудитор описав для «Котушка в комплекті» (DATA-03), але для «Серії» він її не назвав: фільтр формально з чотирьох значен |
| дані | M | Картка каталогу має коротку назву товару — «Kingroon PLA — Чорний (Black)», «Sunlu PLA Silk — Золотий (Gold)» (артборди Category і Landing) | Назви товарів у базі — довгі SEO-рядки: `reports/taxonomy-report.json` → «Філамент (пластик для 3D принтера) Kingroon PETG (CoPET) 1,75 мм 3 кг», `reports/color-report.json` → «Філамент (пластик для 3D принтера) Kingroon PLA Silk Rainbow 1,75 мм 1 кг». Міграція будує ім'я варіанта як `${product.name} — ${color.name_uk}` (`scripts/migrations/normalize-variant-colors.js:262`), а картка друкує його дослівно з `line-clam |
