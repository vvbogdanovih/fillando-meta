# Cloned component repositories (managed via repos.manifest + scripts/clone-all.sh)
# Each keeps its own git remote and history; never committed here.
/repos/

# Claude Code per-user local settings
.claude/

# Environment files — never commit secrets
/.env
/.env.local
/.env.*.local

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
